<section class="container py-5">
  <div class="card card-body shadow-lg border-0">
    <div class="d-flex justify-content-between align-items-center mb-3">
      <h2 class="d-flex align-items-center gap-2">
        <span class="material-icons">campaign</span> Listar Anúncios
      </h2>
      <a href="/anuncios/adicionar" class="btn btn-primary d-flex align-items-center gap-2">
        <span class="material-icons">add</span> Cadastrar Anúncio
      </a>
    </div>
    <hr>
    <div class="table-responsive">
      <table class="table table-striped table-hover align-middle">
        <thead>
          <tr>
            <th>
                <span class="d-flex align-items-center gap-2">
                <span class="material-icons" aria-hidden="true">description</span>
                Título
                </span>
            </th>
            <th>
                <span class="d-flex align-items-center gap-2">
                <span class="material-icons" aria-hidden="true">flag</span>
                Status
                </span>
            </th>
            <th>
                <span class="d-flex align-items-center gap-2">
                <span class="material-icons" aria-hidden="true">notes</span>
                Descrição
                </span>
            </th>
            <th class="text-center">
                <span class="d-flex align-items-center gap-2 justify-content-center">
                <span class="material-icons" aria-hidden="true">settings</span>
                Ações
                </span>
            </th>
          </tr>
        </thead>
        <tbody>
          <?php foreach ($anuncios as $cada): ?>
            <tr>
              <td><?= htmlspecialchars($cada->getTitle()) ?></td>
              <td>
                <span class="badge badge-draft">
                  <?php
                    // Se status for Enum, pega o value, senão mostra como string
                    $status = $cada->getStatus();
                    echo is_object($status) && property_exists($status, 'value')
                      ? htmlspecialchars($status->value)
                      : htmlspecialchars($status);
                  ?>
                </span>
              </td>
              <td><?= htmlspecialchars($cada->getDescription()) ?></td>
              <td>
                <div class="d-flex justify-content-center gap-2">
                  <a href="/anuncios/editar/<?= $cada->getId() ?>" class="btn btn-primary btn-sm d-flex align-items-center gap-1">
                    <span class="material-icons">edit</span> Editar
                  </a>
                  <a href="/anuncios/excluir/<?= $cada->getId() ?>" class="btn btn-danger btn-sm d-flex align-items-center gap-1">
                    <span class="material-icons">delete</span> Excluir
                  </a>
                </div>
              </td>
            </tr>
          <?php endforeach; ?>
        </tbody>
      </table>
    </div>
  </div>
</section>
